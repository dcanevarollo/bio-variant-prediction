# Setup

```bash
git clone https://github.com/user/bio-variant-prediction.git

cd bio-variant-prediction

mamba env create -f environment.yml

mamba activate bio-variant-prediction

pip install -e .
```
